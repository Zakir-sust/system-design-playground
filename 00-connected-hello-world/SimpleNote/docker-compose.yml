


services:
  mydb:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: firstdb
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/bar/lib/postgres/data

volumes:
  pgdata: